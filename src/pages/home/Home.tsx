import BasePageLayout from '../../layouts/BasePageLayout';

const Home = () => {
    return (
        <BasePageLayout barraDeFerramentas={<></>}>
        </BasePageLayout>
    );
};

export default Home;